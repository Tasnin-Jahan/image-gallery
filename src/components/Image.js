// import React from 'react';

// const Image = ({ data, selected, isFeature, onClick }) => {
//     const { id, img } = data;

//     return (
//         <div
//             className={`image ${selected ? 'selected' : ''} ${isFeature ? 'feature' : ''}`}
//             onClick={() => onClick(id)}
//         >
//             <img src={img} alt={`Image ${id}`} />
//         </div>
//     );
// };

// export default Image;